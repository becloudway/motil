{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "bootstrap": "npx lerna bootstrap",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "lerna": "^3.3.1",
    "tslint": "^5.11.0",
    "typescript": "^3.2.1"
  }
}
